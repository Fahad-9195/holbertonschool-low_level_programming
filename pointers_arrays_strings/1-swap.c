#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
a = 5
b = 10
a = a + b
b = a - b
a = a - b
 printf("a = %d, b = %d\n", a, b);
    return 0;
}

